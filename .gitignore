# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/
test-preferences.json

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase emulator logs (created by `npm run emulators`)
firebase-debug.log
firestore-debug.log
ui-debug.log
pubsub-debug.log
auth-debug.log
functions-debug.log
database-debug.log
storage-debug.log

# Firebase emulator export/import data (seeded local dev data)
emulator-data/

# Firebase Data Connect emulator runtime artifacts (generated schema + local Postgres data)
dataconnect/.dataconnect/

# The Firebase Data Connect generated SDK IS committed since the repository
# parity layer (lib/server/sqlconnect-stores.ts) imports it, so CI typechecks
# against the real surface. Regenerate with `firebase dataconnect:compile`
# after connector edits and commit the refreshed output.
# local env files
.env*.local

# Freebuff scratch artifacts (agent probe scripts + PR description drafts)
.freebuff/
.pr-body-*.md

# typescript
*.tsbuildinfo
next-env.d.ts
